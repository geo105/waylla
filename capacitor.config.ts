import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'pe.waylla.campo',
  appName: 'Waylla Campo',
  webDir: 'dist',
  android: {
    backgroundColor: '#f2f4f3',
  },
}

export default config
